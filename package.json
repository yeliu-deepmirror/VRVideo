{
  "name": "minimal_vr",
  "version": "0.0.0",
  "description": "minimal three.js VR app",
  "main": "index.js",
  "scripts": {
    "start": "webpack-dev-server --open --env.NODE_ENV=dev --host 0.0.0.0 --port 3000 --https",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx webpack --config webpack.config.js --env.NODE_ENV=prod"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "three": "^0.148.0",
    "timingsrc": "^1.4.10"
  },
  "devDependencies": {
    "html-webpack-plugin": "^4.3.0",
    "webpack": "^5.43.0",
    "webpack-cli": "^3.3.11",
    "webpack-dev-server": "^3.10.3"
  }
}
